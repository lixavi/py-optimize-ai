# Default configuration settings for the program

DEFAULT_CONFIG = {
    "optimized_files_directory": "optimized_files",  # Directory to store optimized files
    "max_line_length": 80,  # Maximum allowed line length
    "unused_imports": True,  # Enable/disable removal of unused imports
    # Add more configuration settings as needed
}
