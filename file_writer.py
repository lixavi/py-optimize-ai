import os

def write_files(code_files, directory):
    """
    Write optimized code to files in the specified directory.

    Args:
    - code_files (dict): A dictionary containing file names as keys and optimized code as values.
    - directory (str): The directory path where the files will be written.
    """
    if not os.path.exists(directory):
        os.makedirs(directory)
    
    for file_name, code_content in code_files.items():
        file_path = os.path.join(directory, file_name)
        with open(file_path, 'w') as file:
            file.write(code_content)

        print(f"Optimized code written to '{file_path}'")

    if not os.path.exists(directory):
        os.makedirs(directory)
    
    for file_name, code_content in code_files.items():
        file_path = os.path.join(directory, file_name)
        with open(file_path, 'w') as file:
            file.write(code_content)

        print(f"Optimized code written to '{file_path}'")
    if not os.path.exists(directory):
        os.makedirs(directory)
    
    for file_name, code_content in code_files.items():
        file_path = os.path.join(directory, file_name)
        with open(file_path, 'w') as file:
            file.write(code_content)

        print(f"Optimized code written to '{file_path}'")
    if not os.path.exists(directory):
        os.makedirs(directory)
    
    for file_name, code_content in code_files.items():
        file_path = os.path.join(directory, file_name)
        with open(file_path, 'w') as file:
            file.write(code_content)

        print(f"Optimized code written to '{file_path}'")
    if not os.path.exists(directory):
        os.makedirs(directory)
    
    for file_name, code_content in code_files.items():
        file_path = os.path.join(directory, file_name)
        with open(file_path, 'w') as file:
            file.write(code_content)

        print(f"Optimized code written to '{file_path}'")
    if not os.path.exists(directory):
        os.makedirs(directory)
    
    for file_name, code_content in code_files.items():
        file_path = os.path.join(directory, file_name)
        with open(file_path, 'w') as file:
            file.write(code_content)

        print(f"Optimized code written to '{file_path}'")
    if not os.path.exists(directory):
        os.makedirs(directory)
    
    for file_name, code_content in code_files.items():
        file_path = os.path.join(directory, file_name)
        with open(file_path, 'w') as file:
            file.write(code_content)

        print(f"Optimized code written to '{file_path}'")
    if not os.path.exists(directory):
        os.makedirs(directory)
    
    for file_name, code_content in code_files.items():
        file_path = os.path.join(directory, file_name)
        with open(file_path, 'w') as file:
            file.write(code_content)

        print(f"Optimized code written to '{file_path}'")
    if not os.path.exists(directory):
        os.makedirs(directory)
    
    for file_name, code_content in code_files.items():
        file_path = os.path.join(directory, file_name)
        with open(file_path, 'w') as file:
            file.write(code_content)

        print(f"Optimized code written to '{file_path}'")
    if not os.path.exists(directory):
        os.makedirs(directory)
    
    for file_name, code_content in code_files.items():
        file_path = os.path.join(directory, file_name)
        with open(file_path, 'w') as file:
            file.write(code_content)

        print(f"Optimized code written to '{file_path}'")
    if not os.path.exists(directory):
        os.makedirs(directory)
    
    for file_name, code_content in code_files.items():
        file_path = os.path.join(directory, file_name)
        with open(file_path, 'w') as file:
            file.write(code_content)

        print(f"Optimized code written to '{file_path}'")
    if not os.path.exists(directory):
        os.makedirs(directory)
    
    for file_name, code_content in code_files.items():
        file_path = os.path.join(directory, file_name)
        with open(file_path, 'w') as file:
            file.write(code_content)

        print(f"Optimized code written to '{file_path}'")
    if not os.path.exists(directory):
        os.makedirs(directory)
    
    for file_name, code_content in code_files.items():
        file_path = os.path.join(directory, file_name)
        with open(file_path, 'w') as file:
            file.write(code_content)

        print(f"Optimized code written to '{file_path}'")
    if not os.path.exists(directory):
        os.makedirs(directory)
    
    for file_name, code_content in code_files.items():
        file_path = os.path.join(directory, file_name)
        with open(file_path, 'w') as file:
            file.write(code_content)

        print(f"Optimized code written to '{file_path}'")
    if not os.path.exists(directory):
        os.makedirs(directory)
    
    for file_name, code_content in code_files.items():
        file_path = os.path.join(directory, file_name)
        with open(file_path, 'w') as file:
            file.write(code_content)

        print(f"Optimized code written to '{file_path}'")
    if not os.path.exists(directory):
        os.makedirs(directory)
    
    for file_name, code_content in code_files.items():
        file_path = os.path.join(directory, file_name)
        with open(file_path, 'w') as file:
            file.write(code_content)

        print(f"Optimized code written to '{file_path}'")
